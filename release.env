# GitHub repo the proxy downloads mod/proxy updates from (owner/name).
# Used by build_proxy.bat and GitHub Actions (CI overrides with github.repository).
EW_GITHUB_REPO=chloelcdev/noita_entangled_worlds
